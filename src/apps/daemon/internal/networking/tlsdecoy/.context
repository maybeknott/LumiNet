# src/apps/daemon/internal/networking/tlsdecoy

## Purpose
Daemon tlsdecoy implementation module.

## Architectural role
- Daemon dependency band: `networking` (rank 2; lower rank = deeper implementation).
- Cross-band imports may point only to a lower rank. Same-band imports are allowed when they remain within one architectural layer.

## Contents
- Direct files (16): `adaptive_strategy_racer.go`, `adaptive_strategy_racer_test.go`, `combined_bypass.go`, `combined_bypass_test.go`, `decoy.go`, `decoy_test.go`, `multi_strategy_fragmenter.go`, `multi_strategy_fragmenter_test.go`, `server_hello.go`, `server_hello_test.go`, `sni_fragment_conn.go`, `sni_fragment_conn_test.go` …

## Interfaces and dependencies
- Go package: `tlsdecoy`.
- Notable exported surface: `AdaptiveStrategyRacer`, `BuildClientResponseWith`, `BuildDisposableFakeProbe`, `BuildFakeClientHello`, `BuildFakePayloadPacket`, `BuildPaddedClientHello`, `BuildServerHelloWith`, `BuildTlsRecordFrame`, `BypassMode`, `CarrierEdgeProfile` …
- Daemon-local dependencies: none.
- Direct daemon callers: `internal/analysis/diagnostics`, `internal/runtime/proxy`.

## Invariants
- `networking` code must not import a higher-rank daemon band.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
