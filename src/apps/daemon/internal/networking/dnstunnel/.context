# src/apps/daemon/internal/networking/dnstunnel

## Purpose
Daemon dnstunnel implementation module.

## Architectural role
- Daemon dependency band: `networking` (rank 2; lower rank = deeper implementation).
- Cross-band imports may point only to a lower rank. Same-band imports are allowed when they remain within one architectural layer.

## Contents
- Direct files (11): `deployment_plan.go`, `deployment_plan_test.go`, `hardware_advisor.go`, `hardware_advisor_test.go`, `mtu_probe.go`, `mtu_probe_test.go`, `plan.go`, `plan_test.go`, `reliability.go`, `resolver_balancer.go`, `resolver_balancer_test.go`

## Interfaces and dependencies
- Go package: `dnstunnel`.
- Notable exported surface: `AdviseClient`, `AdviseServer`, `Balancer`, `BalancerMode`, `BuildDeploymentPlan`, `BuildPlan`, `ClientHardwareProfile`, `ClientRecommendation`, `DNSRecordPlan`, `DeploymentPlan` …
- Daemon-local dependencies: none.
- Direct daemon callers: `internal/adapters/api`.

## Invariants
- `networking` code must not import a higher-rank daemon band.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
