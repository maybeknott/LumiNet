# src/apps/daemon/internal/networking/relay

## Purpose
Daemon relay implementation module.

## Architectural role
- Daemon dependency band: `networking` (rank 2; lower rank = deeper implementation).
- Cross-band imports may point only to a lower rank. Same-band imports are allowed when they remain within one architectural layer.

## Contents
- Direct files (18): `acl_filter.go`, `acl_filter_test.go`, `edge_router.go`, `edge_router_test.go`, `mesh_peer_matrix.go`, `mesh_peer_matrix_test.go`, `multipath_dedup.go`, `multipath_dedup_test.go`, `multipath_gateway.go`, `multipath_gateway_test.go`, `programmable_proxy_pipeline.go`, `programmable_proxy_pipeline_test.go` …

## Interfaces and dependencies
- Go package: `relay`.
- Notable exported surface: `BlockPathHook`, `BuildDohAQuery`, `BuildUdpResponsePacket`, `CalculateLinkCost`, `ChannelKey`, `DecodeUdpOverTcpFrame`, `DecodeUdpResponsePacket`, `DefaultEdgeRelayConfig`, `EdgeRelayConfig`, `EdgeRelayRouter` …
- Daemon-local dependencies: none.
- Direct daemon callers: none detected.

## Invariants
- `networking` code must not import a higher-rank daemon band.

## Navigation
- Read this file first, then direct child `.context` files before editing a deeper folder.
- Update this context when ownership, supported interfaces, or child-folder meaning changes; do not use it as a changelog.
